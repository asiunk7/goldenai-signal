services:
  - type: web
    name: goldenai-signal
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:app --bind 0.0.0.0:$PORT"
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
